import { PopoverInst, PopoverTrigger } from "./src/interface.js";
import _default, { PopoverProps, PopoverSlots, popoverProps } from "./src/Popover.js";
import { FollowerPlacement as PopoverPlacement } from "vueuc";
export { _default as NPopover, type PopoverInst, type PopoverPlacement, type PopoverProps, type PopoverSlots, type PopoverTrigger, popoverProps };